/**
 * Blog types and API. For types only (client-safe). For data loading use dynamic import of blog-content in getStaticProps/getServerSideProps only.
 */
export type { BlogPost } from "./blog-types";
